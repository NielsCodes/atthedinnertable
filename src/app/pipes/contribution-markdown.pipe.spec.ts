import { ContributionMarkdownPipe } from './contribution-markdown.pipe';

describe('ContributionMarkdownPipe', () => {
  it('create an instance', () => {
    const pipe = new ContributionMarkdownPipe();
    expect(pipe).toBeTruthy();
  });
});
