import { TwitterSigninDirective } from './twitter-signin.directive';

describe('TwitterSigninDirective', () => {
  it('should create an instance', () => {
    const directive = new TwitterSigninDirective();
    expect(directive).toBeTruthy();
  });
});
